<!--<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="najtek.database.dao.user.UserRoleMapper">

	<typeAlias type=”najtek.domain.user.UserRole” alias=”UserRole”/>
	
	<select id="selectById" parameterType="UserRole" 
			resultType="UserRole">
		select * from user_role where user_id = #{userId}
	</select>
	
	<insert id="insert" parameterType="UserRole"
			useGeneratedKeys=”true” keyProperty=”id”>
		insert into user_role (user_id, role) values (#{userId}, #{role})
	</insert>	
	
	<delete id="delete” parameterType="long">
		delete from user_role where id = #{id}
	</delete>
</mapper>-->
